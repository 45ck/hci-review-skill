policy:
  allow_implicit_invocation: false

interface:
  display_name: "Consistency Audit + Beads"
  short_description: "Audit cross-screen consistency and create Beads issues from findings."
  default_prompt: "Audit cross-screen consistency on the current project and create Beads issues from findings."
