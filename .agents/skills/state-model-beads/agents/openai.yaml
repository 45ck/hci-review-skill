policy:
  allow_implicit_invocation: false

interface:
  display_name: "State Model + Beads"
  short_description: "Define state machines and create Beads issues from findings."
  default_prompt: "Define state machines for the current project and create Beads issues from findings."
